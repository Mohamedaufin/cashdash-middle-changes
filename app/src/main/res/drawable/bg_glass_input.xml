<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- FOCUSED / SELECTED STATE -->
    <item android:state_focused="true">
        <layer-list>
            <!-- Base: Surface 2 -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/surface_2" />
                    <corners android:radius="16dp" />
                    <stroke
                        android:width="2dp"
                        android:color="@color/focus_ring" />
                </shape>
            </item>
            <!-- Subtle Inner Glow for focus -->
            <item android:top="2dp" android:left="2dp" android:right="2dp" android:bottom="20dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:startColor="#154DE1C1"
                        android:endColor="#00000000"
                        android:angle="270" />
                    <corners android:radius="14dp" />
                </shape>
            </item>
        </layer-list>
    </item>

    <item android:state_selected="true">
        <layer-list>
            <!-- Base: Surface 2 -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/surface_2" />
                    <corners android:radius="16dp" />
                    <stroke
                        android:width="2.5dp"
                        android:color="@color/focus_ring" />
                </shape>
            </item>
            <!-- Vibrant Inner Glow for selection -->
            <item android:top="2dp" android:left="2dp" android:right="2dp" android:bottom="20dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:startColor="#254DE1C1"
                        android:endColor="#00000000"
                        android:angle="270" />
                    <corners android:radius="14dp" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- DEFAULT STATE -->
    <item>
        <layer-list>
            <!-- Base: Surface 2 (Input fields, chips) -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/surface_2" />
                    <corners android:radius="16dp" />
                    <stroke
                        android:width="1.2dp"
                        android:color="@color/border" />
                </shape>
            </item>
            <!-- Subdued inner glow -->
            <item>
                <shape android:shape="rectangle">
                    <gradient
                        android:startColor="#0AFFFFFF"
                        android:endColor="#00FFFFFF"
                        android:angle="270" />
                    <corners android:radius="16dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>