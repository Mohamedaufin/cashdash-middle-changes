<?xml version="1.0" encoding="utf-8"?>
<!--
  White theme card.

  Black outline, deliberately. It is the app's look on this theme, not an oversight, so
  do not "soften" it to a grey hairline.

  The fill is the part that changed. It was #E8ECF0, darker than the page it sat on
  (1.08:1), which left the outline doing all the work of separating the card from the
  background - take the border away and the card disappeared. Now white on the grey page
  from bg_main_gradient_white, so the card is already a raised surface and the outline is
  a style choice on top of that rather than the only thing holding it together.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF"/>
    <corners android:radius="20dp"/>
    <stroke
        android:width="1dp"
        android:color="#000000"/>
</shape>
