<?xml version="1.0" encoding="utf-8"?>
<!-- Glyph inside the intro's continue button. Fixed dark rather than themed, because it
     always sits on the accent, which is the same on all three themes. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="120"
    android:viewportHeight="120">
    <path
        android:pathData="M 46,26 L 80,60 L 46,94"
        android:strokeColor="@color/intro_on_accent"
        android:strokeWidth="12"
        android:strokeLineCap="round"
        android:strokeLineJoin="round" />
</vector>
