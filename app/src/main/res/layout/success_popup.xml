<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/mainBackground">

    <!-- Success Glow Circle -->
    <View
        android:id="@+id/glowCircle"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:background="@drawable/bg_glass_3d_round"
        android:alpha="0.4"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="100dp"/>

    <!-- Checkmark Icon -->
    <ImageView
        android:id="@+id/ivCheck"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/ic_history"
        android:tint="#00FF88"
        app:layout_constraintTop_toTopOf="@id/glowCircle"
        app:layout_constraintBottom_toBottomOf="@id/glowCircle"
        app:layout_constraintStart_toStartOf="@id/glowCircle"
        app:layout_constraintEnd_toEndOf="@id/glowCircle"
        android:padding="15dp"
        android:translationZ="12dp"/>

    <TextView
        android:id="@+id/tvSuccess"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Success"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/text_headline"
        android:textStyle="bold"
        android:letterSpacing="0.2"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/glowCircle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- DETAILS PANEL -->
    <LinearLayout
        android:id="@+id/detailsPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="30dp"
        android:background="?attr/panelBackground"
        app:layout_constraintTop_toBottomOf="@id/tvSuccess">

        <TextView
            android:id="@+id/paymentMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Transaction Detected!"
            android:textSize="@dimen/text_subhead"
            android:textColor="#B0C8FF"
            android:gravity="center"
            android:lineSpacingExtra="4dp"/>
    </LinearLayout>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnDone"
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:layout_marginHorizontal="30dp"
        android:layout_marginBottom="50dp"
        android:text="Dismiss"
        android:textColor="#FFFFFF"
        android:textSize="@dimen/text_subhead"
        android:textStyle="bold"
        android:background="?attr/inputBackground"
        android:backgroundTint="@null"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
