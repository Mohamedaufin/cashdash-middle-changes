*.iml
.gradle
/local.properties
/.idea/
/.kotlin/
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.log
hs_err_pid*
replay_pid*

# Specific to this project - ignoring other modules/folders
/cashdash_flutter/
/test_build/
/Cashdash1111/
# /functions/ (Removed from ignore to track backend changes)

# Android app artifacts
/app/build/
/app/release/
*.apk
*.ap_
*.dex
*.class
*.jks

# Captured device logs and crash dumps — these contained real user emails (PII).
# Never commit these; they are working files only.
logcat*.txt
current_logcat.txt
crash.txt
all_history.txt
themes_git.txt
*_dump.txt

# Scratch / superseded working copies
old_*.kt
old_*.xml
temp_*.kt
temp_*.xml

# One-off source-rewriting scripts (see replace.py)
replace.py

# Stray root lockfile. The real one is functions/package-lock.json; this gets
# recreated by any npm command run from the repo root and was untracked once
# already, so ignore it rather than let `git add .` pull it back in.
/package-lock.json
